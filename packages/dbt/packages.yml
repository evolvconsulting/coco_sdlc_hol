# =============================================================================
# EVOLV PERFORMANCE INTELLIGENCE - dbt Packages
# =============================================================================

packages:
  # dbt utilities package for common macros
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
