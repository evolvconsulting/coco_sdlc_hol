---
phase: 04-instructor-reference-sheet-with-prompt-by-prompt-sequence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - INSTRUCTOR_GUIDE.md
autonomous: false
requirements: [CONTEXT-04]

must_haves:
  truths:
    - "Every participant-typed input from LAB_INSTRUCTIONS.md appears in sequence"
    - "Step numbers match LAB_INSTRUCTIONS.md exactly (2.1-2.3, 3.3-3.5, 4.1-4.12 with sub-steps, 5, 6.1-6.7)"
    - "Timing cues at each section header match LAB_INSTRUCTIONS.md estimates"
    - "Watch for: callouts appear at all 12 identified sticking-point steps"
    - "Call out to group: notes appear at all 6 identified key transitions"
    - "Conditional/fallback prompts included for steps 4.3 (scope expansion), 4.5, 4.6 (materialization), 6.5 (debug)"
    - "Expected outputs present only for verification steps: 4.10a, 4.10b, 4.10c, 4.10d, 6.5"
    - "EPA-2 and EPA-3 used as ticket IDs (not placeholders)"
    - "No participant-facing explanatory prose — every non-code line is a step label or callout"
    - "Quick-reference troubleshooting table present at end"
  artifacts:
    - path: "INSTRUCTOR_GUIDE.md"
      provides: "Instructor-facing live-use reference sheet"
      contains: "Section 2, Section 3, Section 4, Section 5, Section 6"
  key_links:
    - from: "INSTRUCTOR_GUIDE.md step numbers"
      to: "LAB_INSTRUCTIONS.md step numbers"
      via: "exact match — no renumbering or collapsing"
      pattern: "Step 4\\.10[abcd]"
---

<objective>
Produce INSTRUCTOR_GUIDE.md at the repo root — a standalone instructor-facing live reference sheet listing every participant-typed input in sequence across the full lab, organized to match LAB_INSTRUCTIONS.md section/step structure, with timing cues, Watch for: callouts at sticking points, and Call out to group: notes at key transitions.

Purpose: Enable a facilitator to track exactly where participants are step-by-step during the 90-minute lab without needing to reference LAB_INSTRUCTIONS.md, while having inline guidance for the moments most likely to cause participant confusion.

Output: INSTRUCTOR_GUIDE.md at repo root (~150-200 lines, dense/scannable format)
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-instructor-reference-sheet-with-prompt-by-prompt-sequence/04-CONTEXT.md
@.planning/phases/04-instructor-reference-sheet-with-prompt-by-prompt-sequence/04-RESEARCH.md

@LAB_INSTRUCTIONS.md
@INFRASTRUCTURE.md

<interfaces>
<!-- Source material: all participant inputs and structure have been pre-extracted in 04-RESEARCH.md -->
<!-- See: ## Source Material Inventory section of RESEARCH.md for the complete catalog -->
<!-- See: ## Facilitation Callout Analysis section for Watch for: and Call out to group: assignments -->
<!-- See: ## Architecture Patterns section for document structure and entry format pattern -->
<!-- See: ## Common Pitfalls section — all 5 pitfalls must be avoided -->
<!-- See: ## Don't Hand-Roll section — copy verbatim, never paraphrase -->

<!-- LOCKED entry format pattern (from CONTEXT.md):

**Step X.Y — [1-line action label]**
```
[exact participant input verbatim]
```
> Watch for: [callout — only at known sticking points]
> Call out to group: [callout — only at key transitions]

Input type coding:
- Cortex Code natural language: plain fenced block (no language tag)
- bash/terminal command: ```bash fenced block
- Cortex Code slash command: ```cortex fenced block (or plain, treated uniformly)

Expected outputs: separate fenced block immediately after input block, ONLY for verification steps
(steps 4.10a, 4.10b, 4.10c, 4.10d, 6.5)

Conditional/fallback prompts: bracketed label before the code block, e.g.:
[If plan misses intermediate model or agent file]
```
[fallback prompt verbatim]
```
-->

<!-- CONFIRMED TICKET IDs (live from Phase 3):
- EPA-2 = retry success rate story (Ticket 1)
- EPA-3 = KPI card story (Ticket 2)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write document header and Sections 1-3</name>
  <files>INSTRUCTOR_GUIDE.md</files>
  <action>
Create INSTRUCTOR_GUIDE.md at the repo root. Write the following sections in order:

**Document header:**
- Title: "Instructor Reference Guide — AI-Assisted SDLC HOL"
- 2-3 line purpose statement: instructor-only, live-use reference, companion to LAB_INSTRUCTIONS.md and INFRASTRUCTURE.md
- How to use: one-line note that step numbers mirror LAB_INSTRUCTIONS.md exactly
- Total lab duration: ~90 min

**Section 1: Architecture Overview (~10 min)**
- Section header with timing cue
- Single line: "Instructor-led architecture walkthrough — no participant inputs."
- (Pitfall 4 in RESEARCH.md: must include this header even though there are no participant inputs, so facilitator can track total session time)

**Section 2: Environment Setup Verification (~5 min)**
- Section header with timing cue

Step 1 — Confirm Snowflake Connection:
```bash
snow sql -c ennovate -q "SELECT CURRENT_ROLE(), CURRENT_DATABASE(), CURRENT_SCHEMA();"
```
Expected output block (copy verbatim from LAB_INSTRUCTIONS.md Step 1):
```
+----------------+--------------------+------------------+
| CURRENT_ROLE() | CURRENT_DATABASE() | CURRENT_SCHEMA() |
+----------------+--------------------+------------------+
| ATTENDEE_ROLE  | COCO_SDLC_HOL      | MARTS            |
+----------------+--------------------+------------------+
```
> Watch for: Role must be ATTENDEE_ROLE — if SYSADMIN, connection profile is wrong.

Step 2 — Confirm local app runs:
```bash
cd apps/frontend && npm install && npm run dev
```
Expected output: `> Ready on http://localhost:3000`
> Watch for: Dashboard loads but shows no data — check `.env.local` SNOWFLAKE_ACCOUNT value.

Step 3 — Confirm Cortex Code CLI installed:
```bash
cortex --version
```
Expected output: version string (e.g., `cortex 1.x.x`)
> Watch for: "command not found" after install — instruct to restart terminal.

[If not installed — fallback]:
```bash
curl -LsS https://ai.snowflake.com/static/cc-scripts/install.sh | sh
```

**Section 3: Cortex Code Primer (~10 min)**
- Section header with timing cue
- Note: Sections 3.1 and 3.2 are instructor-led explanations — no participant inputs.

Step 3.3 — Install Jira MCP skill:
```bash
cortex mcp add jira --url https://evolv-coco-sdlc-hol.atlassian.net --auth-token ATATT3xFfGF0D7Aiugi8RrvbyL4UHnMz-wrOpVZkykXnM7OQcUWgruzWN1HreG_iWhaVD9vfsuE_ZAtDIgTHG3xjRmue861sVE3v2nVs1_uqhjQ_XRsx4eSKVV1Zr8FFLZ1BMOdtusft0jPXZcrZkzmbA_KfOLjXOGDWqoNiKFkw-bRxuM5-iCU=64649D40
```
[Alternative — interactive]:
```
/mcp
```
(then follow prompts to add Jira server)

Step 3.4 — Install Confluence MCP skill:
```bash
cortex mcp add confluence --url https://evolv-coco-sdlc-hol.atlassian.net --auth-token ATATT3xFfGF0JmTTc6yxUOmZbKA0ZlbDtsH8KZv3pijAYQ3Su0tUGnz7xODTQiYe16J1Xvz7nl6o-GtkOgkX0LWGcl-VcjygrFz9KNcAqDJqvOZlNyvmGn_ozYe5Bedn8QRqi2_nAMOaUNniftWkIYqNrHke4d09m0BnOJGfpUdLDOjwO-TWDq0=363884CD
```

Step 3.5 — Quick test — verify Cortex Code reads repo context:
```bash
cortex
```
Then in Cortex Code:
```
What database and schema does this project use?
```
Expected behavior: Response mentions `COCO_SDLC_HOL` and the medallion architecture (RAW, STAGING, INTERMEDIATE, MARTS).
> Watch for: Response is generic and doesn't mention COCO_SDLC_HOL — Cortex Code must be launched from repo root, not a subdirectory.

STYLE RULES (critical — read before writing):
- Every entry is a step label + code block + optional callout. NO explanatory prose between steps.
- Do not copy any explanatory text from LAB_INSTRUCTIONS.md (e.g., "This confirms your Snowflake CLI is configured..." — omit entirely).
- Match INFRASTRUCTURE.md tone: direct, imperative, no hand-holding.
- Timing cues appear only at section headers, not per-step.
  </action>
  <verify>
File INSTRUCTOR_GUIDE.md exists at repo root. Contains sections 1, 2, and 3. Section headers include timing cues. Steps 1, 2, 3 from Section 2 and steps 3.3, 3.4, 3.5 from Section 3 are present with code blocks. Watch for: callouts at steps 1, 2, 3, and 3.5. Expected output blocks at steps 1, 2, 3, and 3.5. No explanatory paragraph prose between steps.
  </verify>
  <done>Sections 1-3 written in scannable step/callout format, no participant-facing prose, step numbers matching LAB_INSTRUCTIONS.md exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 4 (Task 1 — full sequence) and Section 5 (context switch)</name>
  <files>INSTRUCTOR_GUIDE.md</files>
  <action>
Append to INSTRUCTOR_GUIDE.md. Write Section 4 and Section 5.

**Section 4: Task 1 — Add Retry Success Rate Metric (~30 min)**
- Section header with timing cue

Step 4.1 — Launch Cortex Code and read ticket:
```bash
cortex
```
Then in Cortex Code:
```
Show me Jira ticket EPA-2. What does it ask me to implement?
```

Step 4.2 — Create feature branch:
```
Create a new git branch called feature/retry-success-rate and switch to it.
```

Step 4.3 — Enable plan mode and describe task:
```
/plan
```
Then:
```
I need to add a retry success rate metric to the authorizations domain. Retry success rate = count of transactions where a customer was initially declined then approved on a subsequent attempt. Add this to the dbt mart, the semantic view, and update the Cortex Agent instructions. Start by reading the relevant files.
```
> Watch for: Plan shows only 2-3 files — prompt to expand scope.

[If plan misses intermediate model or agent file]:
```
Please also include changes to the intermediate model (int_authorizations__enriched.sql) for the retry detection logic and the Cortex Agent instructions (03_create_agent.sql).
```
> Call out to group: Plan mode — Cortex Code shows what it will do before doing it. This is your review gate.

Step 4.4 — Confirm and execute plan:
```
The plan looks good. Execute it.
```

Step 4.5 — dbt model update (if retry logic missing):

[If Cortex Code's plan doesn't include retry detection SQL]:
```
Add the retry detection logic as a window function in int_authorizations__enriched.sql. A retry is when the same card_bin, card_last_four, and transaction_amount appear from the same merchant within 5 minutes of a declined transaction. Add retry_attempt_flag and retry_success_flag columns.
```

Step 4.6 — dbt materialization check (no participant input):
> Watch for: Any suggestion to change materialization type — existing types (staging=view, intermediate=dynamic table, marts=dynamic table) must be preserved.

Step 4.7 — dbt unit tests (no participant input):
_(Cortex Code may update tests automatically — review for correctness. No manual prompt needed in lab environment.)_

Step 4.8 — Review semantic view update (no participant input):
_(Cortex Code applies RETRY_SUCCESS_RATE metric automatically as part of plan execution. Review output for correct metric structure.)_

Step 4.9 — Review Cortex Agent instruction change (no participant input):
_(Cortex Code updates 03_create_agent.sql automatically. Verify instruction text mentions "retry success rate".)_

Step 4.10a — Apply DDL and refresh dynamic tables:
```
Run the compiled CREATE OR REPLACE DYNAMIC TABLE statements from the modified dbt models against Snowflake to apply the new columns. Then refresh both dynamic tables so the data is immediately available: ALTER DYNAMIC TABLE COCO_SDLC_HOL.INTERMEDIATE.INT_AUTHORIZATIONS__ENRICHED REFRESH; and ALTER DYNAMIC TABLE COCO_SDLC_HOL.MARTS.AUTHORIZATIONS REFRESH;
```
Expected behavior: Confirmation that both tables were updated.
> Watch for: Confirmation message missing — without refresh, new columns have no data.

Step 4.10b — Rebuild semantic view and verify:
```
Run the updated semantic view DDL from packages/dbt/analyses/payment_analytics_semantic_view.sql against Snowflake. Then run DESCRIBE SEMANTIC VIEW COCO_SDLC_HOL.MARTS.PAYMENT_ANALYTICS and confirm RETRY_SUCCESS_RATE appears in the metrics list.
```
Expected behavior: RETRY_SUCCESS_RATE appears in DESCRIBE output.
> Watch for: Metric missing — ask Cortex Code to re-run the semantic view DDL manually.
> Call out to group: Metric is now live in the semantic view — Cortex Agent can answer questions about it.

Step 4.10c — Verify metric data:
```
Query the AUTHORIZATIONS mart to verify the retry columns contain data. Calculate successful_retries, total_retries, and retry_success_rate_pct for clnt_id = 'dmcl' over the last 30 days.
```
Expected behavior: Non-null retry_success_rate_pct value (typically 20-80%).
> Watch for: Zero rows — all queries must filter `clnt_id = 'dmcl'`.

Step 4.10d — Test Cortex Agent:
```
What is the retry success rate for the last 30 days?
```
Expected behavior: Cortex Agent generates SQL using RETRY_SUCCESS_RATE metric and returns a meaningful answer.
> Call out to group: Full stack connected — one natural language question, Cortex generates the SQL automatically.

Step 4.11 — Commit and push:
```
Commit all changes in packages/dbt/ and packages/database/ with the message "feat(dbt): add retry_success_rate to authorizations mart and semantic view". Then push to origin.
```

Step 4.12 — Reference Confluence data dictionary:
```
Read the Confluence data dictionary page at https://evolv-coco-sdlc-hol.atlassian.net/wiki/spaces/EPA/pages/851970/Data+Dictionary+-+Authorizations. What metrics are currently documented? How should I document the new retry_success_rate metric to match the existing format?
```

---

**Section 5: Context Switch (~2 min)**
- Section header with timing cue

Clear context:
```
/new
```
> Call out to group: Context hygiene — clear stale context before switching tasks. Clean context = better AI suggestions.
> Watch for: After /new, plan mode is off. Participants must re-enable /plan at the start of Task 2.

STYLE RULES:
- Steps 4.6, 4.7, 4.8, 4.9 have no participant-typed inputs — use italicized one-liner notes (not code blocks) to indicate Cortex Code handles these automatically during plan execution. Keep them so the instructor can track the sequence without losing their place.
- Sub-steps 4.10a, 4.10b, 4.10c, 4.10d must remain as separate labeled entries — do NOT collapse into a single "Step 4.10" entry (Pitfall 3 in RESEARCH.md).
- All conditional/fallback prompts (4.3 scope expansion, 4.5 retry logic) are bracketed entries — include them.
  </action>
  <verify>
INSTRUCTOR_GUIDE.md contains Section 4 with steps 4.1 through 4.12 (including sub-steps 4.10a/b/c/d as separate entries). Watch for: callouts at steps 4.3, 4.6, 4.10a, 4.10b, 4.10c. Call out to group: notes at steps 4.3, 4.10b, 4.10d. Conditional prompts present for 4.3 and 4.5. Expected output blocks at steps 4.10a, 4.10b, 4.10c, 4.10d. Section 5 present with /new command and Call out + Watch for notes. EPA-2 used as ticket ID.
  </verify>
  <done>Section 4 complete with full prompt sequence, all facilitation callouts, all conditional prompts, and expected outputs at verification steps. Section 5 complete with context-switch annotation.</done>
</task>

<task type="auto">
  <name>Task 3: Write Section 6 and quick-reference troubleshooting table</name>
  <files>INSTRUCTOR_GUIDE.md</files>
  <action>
Append to INSTRUCTOR_GUIDE.md. Write Section 6 and the closing troubleshooting table.

**Section 6: Task 2 — Add KPI Card to Dashboard (~20 min)**
- Section header with timing cue

Step 6.1 — Read the Jira ticket:
```
Show me Jira ticket EPA-3. What does it ask me to implement?
```

Step 6.2 — Create feature branch:
```
Create a new git branch called feature/retry-success-kpi-card and switch to it.
```

Step 6.3 — Enable plan mode and describe task:
```
/plan
```
Then:
```
Read Jira ticket EPA-3. Look at apps/frontend/src/app/analytics/authorization/page.tsx, apps/frontend/src/components/ui/KPICard.tsx, and apps/frontend/src/types/domain.ts. Add a KPI card that shows the retry_success_rate from the authorization KPIs API. Follow the exact same pattern as the existing KPI cards.
```
> Watch for: Plan order wrong — TypeScript interface (domain.ts) must be updated BEFORE API route and page component. If page.tsx appears first, ask Cortex Code to reorder.
> Watch for: Plan mode is session-scoped — after /new, participants must re-enable /plan. If Cortex Code starts executing immediately, remind them.

Step 6.4 — Execute the plan:
```
The plan looks good. Execute it.
```

Step 6.5 — Verify locally:
```
Start the frontend dev server from apps/frontend.
```
Open http://localhost:3000/analytics/authorization. Expected behavior: "Retry Success Rate" KPI card visible, showing a percentage value with green color indicator.
> Watch for: Card shows 0 or undefined — use debug prompt below.
> Call out to group: Ticket 1 became Ticket 2's data source — same metric, different surface, full SDLC cycle complete.

[If KPI card shows 0]:
```
The retry success rate KPI card is showing 0. Check that the AuthorizationKPIs interface in domain.ts includes retrySuccessRate and that the API route in kpis/route.ts returns the field.
```

Step 6.6 — Commit and push:
```
Commit all changes in apps/frontend/ with the message "feat(frontend): add retry success rate KPI card to authorization dashboard". Then push to origin.
```

Step 6.7 — Create a pull request:
```
Create a GitHub pull request for this branch. Title: "Add retry success rate KPI card". Describe what was changed and why.
```
> Call out to group: Jira ticket read at the start, PR created at the end — full development loop without leaving the terminal.

_Lab complete._

---

**Quick-Reference Troubleshooting**

Condensed from LAB_INSTRUCTIONS.md Appendix A. Match the 7-row table verbatim:

| Issue | Cause | Fix |
|-------|-------|-----|
| `cortex: command not found` | CLI not in PATH | `curl -LsS https://ai.snowflake.com/static/cc-scripts/install.sh \| sh` then restart terminal |
| Dynamic table shows old data | Refreshes on schedule, not on DDL | Ask Cortex Code: `ALTER DYNAMIC TABLE ... REFRESH;` (Step 4.10a) |
| Semantic view metric not found | YAML updated but view not rebuilt | Ask Cortex Code to rerun semantic view DDL (Step 4.10b) |
| KPI card shows 0 or undefined | TypeScript interface not updated | Add `retrySuccessRate: number` to `AuthorizationKPIs` in `domain.ts` |
| Cortex Agent gives generic answer | Agent instructions not updated | Rerun `03_create_agent.sql` with updated `instructions.response` |
| `/plan` mode off after `/new` | Plan mode is session-scoped | Re-enable with `/plan` in each new session |
| Verification query returns empty | Missing `clnt_id` filter | Add `WHERE clnt_id = 'dmcl'` to all verification queries |

STYLE RULES:
- The troubleshooting table is required (Pitfall 5 in RESEARCH.md) — include it even though CONTEXT.md doesn't explicitly call it out.
- Section 7 (Wrap-up) is intentionally omitted — it has no participant inputs (open question resolution from RESEARCH.md: omit).
- "Lab complete." after Step 6.7 closes the document cleanly without wrapping up in participant-facing language.
  </action>
  <verify>
INSTRUCTOR_GUIDE.md contains Section 6 with steps 6.1 through 6.7. Watch for: callouts at steps 6.3 (plan order) and 6.3 (/plan after /new) and 6.5. Call out to group: notes at steps 6.5 and 6.7. Conditional debug prompt present for 6.5. Expected output at step 6.5. EPA-3 used as ticket ID. Quick-reference troubleshooting table present with 7 rows matching LAB_INSTRUCTIONS.md Appendix A.
  </verify>
  <done>Section 6 complete with full prompt sequence, all facilitation callouts, debug prompt for KPI card, expected output at 6.5. Troubleshooting table present. INSTRUCTOR_GUIDE.md is complete and ready for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review of INSTRUCTOR_GUIDE.md</name>
  <action>Review INSTRUCTOR_GUIDE.md against LAB_INSTRUCTIONS.md using the verification steps in how-to-verify. Approve or describe issues found.</action>
  <verify>Human reviewer approves document via resume-signal</verify>
  <done>INSTRUCTOR_GUIDE.md accepted as accurate, complete, and scannable for live instructor use</done>
  <what-built>
    INSTRUCTOR_GUIDE.md — complete instructor reference sheet with:
    - All 7 sections (1-6 with content, Section 1 as header-only)
    - Every participant-typed input from LAB_INSTRUCTIONS.md in sequence
    - Step numbers matching LAB_INSTRUCTIONS.md exactly (including 4.10a/b/c/d sub-steps)
    - Timing cues at section headers
    - Watch for: callouts at 12 sticking-point steps
    - Call out to group: notes at 6 key transitions
    - Conditional/fallback prompts for steps 4.3, 4.5, 4.6, 6.5
    - Expected outputs only at verification steps (4.10a/b/c/d, 6.5)
    - Quick-reference troubleshooting table (7 rows)
    - EPA-2 and EPA-3 as live ticket IDs
  </what-built>
  <how-to-verify>
    1. Open INSTRUCTOR_GUIDE.md in a side-by-side view with LAB_INSTRUCTIONS.md
    2. Confirm all sections present: 1 (header only), 2 (steps 1-3), 3 (steps 3.3-3.5), 4 (steps 4.1-4.12 with 4.10a/b/c/d), 5 (context switch), 6 (steps 6.1-6.7)
    3. Check step numbers in INSTRUCTOR_GUIDE.md match LAB_INSTRUCTIONS.md exactly
    4. Verify EPA-2 appears in step 4.1 and EPA-3 appears in step 6.1 (not placeholder text)
    5. Spot-check 3 Watch for: callouts (suggest: Step 1, Step 4.3, Step 6.5) for accuracy
    6. Spot-check 2 Call out to group: notes (suggest: Step 4.10d, Step 6.7) for appropriateness
    7. Confirm no explanatory paragraph prose between steps (each non-code line is a step label or callout)
    8. Confirm troubleshooting table is present at end with 7 rows
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Manual review checkpoint (Task 4) gates completion. Automated checks:
- `test -f INSTRUCTOR_GUIDE.md` — file exists at repo root
- `grep -c "EPA-2" INSTRUCTOR_GUIDE.md` — returns 1+ (ticket ID present)
- `grep -c "EPA-3" INSTRUCTOR_GUIDE.md` — returns 1+ (ticket ID present)
- `grep -c "Watch for:" INSTRUCTOR_GUIDE.md` — returns 10+ (callouts present)
- `grep -c "Call out to group:" INSTRUCTOR_GUIDE.md` — returns 5+ (group notes present)
- `grep -c "Step 4.10" INSTRUCTOR_GUIDE.md` — returns 4+ (sub-steps not collapsed)
</verification>

<success_criteria>
INSTRUCTOR_GUIDE.md exists at repo root and:
- A facilitator can track any participant's current position by asking "what step are you on?" and looking it up instantly
- Every prompt a participant might need to copy is present verbatim
- Conditional/fallback prompts are present for the 4 known failure modes
- No participant-facing explanation survives — only step labels, code blocks, and instructor callouts
- Human reviewer approves via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-instructor-reference-sheet-with-prompt-by-prompt-sequence/04-01-SUMMARY.md` following the summary template at @C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</output>
