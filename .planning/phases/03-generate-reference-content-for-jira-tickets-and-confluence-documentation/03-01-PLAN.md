---
phase: 03-generate-reference-content-for-jira-tickets-and-confluence-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/jira/EPA-epic.wiki
  - docs/jira/EPA-retry-success-rate.wiki
  - docs/jira/EPA-kpi-card.wiki
  - docs/jira/EPA-backlog-1.wiki
  - docs/jira/EPA-backlog-2.wiki
  - docs/jira/EPA-backlog-3.wiki
  - docs/confluence/data-dictionary.wiki
autonomous: true
requirements: [CONTENT-01, CONTENT-02]

must_haves:
  truths:
    - "Jira .wiki files contain complete story-format content with user story, business context, and business-only acceptance criteria"
    - "Confluence data dictionary covers all 11 existing semantic view metrics plus key dimensions/facts from 7 MARTS tables"
    - "Data dictionary deliberately omits retry_success_rate to create the lab aha moment"
    - "No technical hints, file paths, or implementation steps appear in any Jira ticket"
    - "All .wiki files use pure wiki markup syntax with no markdown"
  artifacts:
    - path: "docs/jira/EPA-epic.wiki"
      provides: "Epic description for Payment Analytics Platform Enhancements"
      contains: "h2. Description"
    - path: "docs/jira/EPA-retry-success-rate.wiki"
      provides: "TICKET-1 story: retry success rate metric"
      contains: "h2. Acceptance Criteria"
    - path: "docs/jira/EPA-kpi-card.wiki"
      provides: "TICKET-2 story: retry success rate KPI card"
      contains: "h2. Acceptance Criteria"
    - path: "docs/jira/EPA-backlog-1.wiki"
      provides: "Backlog item 1 for board realism"
    - path: "docs/jira/EPA-backlog-2.wiki"
      provides: "Backlog item 2 for board realism"
    - path: "docs/jira/EPA-backlog-3.wiki"
      provides: "Backlog item 3 for board realism"
    - path: "docs/confluence/data-dictionary.wiki"
      provides: "Payment Analytics Data Dictionary in Confluence wiki markup"
      contains: "APPROVAL_RATE"
  key_links:
    - from: "docs/jira/EPA-retry-success-rate.wiki"
      to: "HANDS_ON_LAB.md Task 1"
      via: "acceptance criteria alignment"
      pattern: "retry success rate"
    - from: "docs/jira/EPA-kpi-card.wiki"
      to: "HANDS_ON_LAB.md Task 2"
      via: "acceptance criteria alignment"
      pattern: "KPI card"
    - from: "docs/confluence/data-dictionary.wiki"
      to: "packages/dbt/analyses/payment_analytics_semantic_view.sql"
      via: "metric/dimension content accuracy"
      pattern: "APPROVAL_RATE|CHARGEBACK_WIN_RATE|EFFECTIVE_FEE_RATE"
---

<objective>
Generate all reference .wiki content files for Jira tickets (epic + 2 main stories + 3 backlog items) and the Confluence data dictionary page.

Purpose: These files are the source of truth for the Atlassian artifacts that support the hands-on lab. Jira tickets give participants real tickets to work from; the data dictionary (deliberately missing retry_success_rate) creates the "aha" moment in the lab.

Output: 6 Jira .wiki files in docs/jira/ and 1 Confluence .wiki file in docs/confluence/
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generate-reference-content-for-jira-tickets-and-confluence-documentation/03-CONTEXT.md
@.planning/phases/03-generate-reference-content-for-jira-tickets-and-confluence-documentation/03-RESEARCH.md

@HANDS_ON_LAB.md
@INFRASTRUCTURE.md
@packages/dbt/analyses/payment_analytics_semantic_view.sql

<interfaces>
<!-- Source data for content accuracy -->

From RESEARCH.md - 11 existing semantic view metrics (include ALL in data dictionary):
- APPROVAL_RATE, TOTAL_AUTHORIZATION_VOLUME, AVERAGE_TRANSACTION_AMOUNT
- NET_SETTLEMENT_VOLUME, TOTAL_DEPOSITS, EFFECTIVE_FEE_RATE
- CHARGEBACK_VOLUME, CHARGEBACK_WIN_RATE, CHARGEBACK_RATE
- NET_ADJUSTMENTS, RETRIEVAL_FULFILLMENT_RATE

From RESEARCH.md - 7 MARTS tables for dimensions/facts:
- AUTHORIZATIONS, SETTLEMENTS, DEPOSITS, CHARGEBACKS, RETRIEVALS, ADJUSTMENTS, DIM_MERCHANTS

CRITICAL: retry_success_rate must NOT appear in the data dictionary.

From CONTEXT.md - Jira wiki markup patterns:
- h2. for headings, # for ordered lists, * for bold
- {panel:title=...|borderStyle=solid|borderColor=#ccc|bgColor=#FFFFCE} for callouts

From CONTEXT.md - Confluence wiki markup patterns:
- h1., h2. for headings, ||Header||Header|| and |cell|cell| for tables
- {info}, {note} macros for callouts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jira .wiki reference files</name>
  <files>docs/jira/EPA-epic.wiki, docs/jira/EPA-retry-success-rate.wiki, docs/jira/EPA-kpi-card.wiki, docs/jira/EPA-backlog-1.wiki, docs/jira/EPA-backlog-2.wiki, docs/jira/EPA-backlog-3.wiki</files>
  <action>
Create docs/jira/ directory and write 6 pure Jira wiki markup files:

**EPA-epic.wiki** - Epic: "Payment Analytics Platform Enhancements"
- Description of the epic scope: adding new metrics, dashboard improvements, and data quality enhancements for the evolv Payment Analytics platform
- Use {panel} macro for key context about the payment analytics domain
- Labels: platform, analytics, enhancements

**EPA-retry-success-rate.wiki** - Story: "Add retry success rate metric to authorizations domain" (this is TICKET-1 in the lab)
- User story: "As a payment analyst, I want to see a retry success rate metric so that I can understand how many declined transactions are recovered through customer retries."
- Business context: 2-3 paragraphs explaining why retry tracking matters for merchants (revenue recovery, decline management, customer experience)
- Acceptance criteria (BUSINESS-ONLY, ordered list):
  1. A retry success rate metric is available for natural language queries about authorization performance
  2. The metric captures the percentage of declined transactions that were subsequently retried and approved
  3. A "retry" is defined as when the same card is used for the same amount at the same merchant within a short window after a decline
  4. The Cortex Agent can answer questions like "What is our retry success rate?" and "How many declined transactions were recovered?"
  5. The data dictionary is updated to include the new metric definition
- NO technical hints, file paths, SQL, dbt references, or implementation guidance
- Include a {panel:title=Business Definition} explaining what constitutes a retry in payment processing terms
- Priority: High, Labels: dbt, semantic-view, metric, cortex-agent

**EPA-kpi-card.wiki** - Story: "Add retry success rate KPI card to authorization dashboard" (this is TICKET-2 in the lab)
- User story: "As a payment analyst, I want to see the retry success rate prominently displayed on the authorization dashboard so that I can monitor retry performance at a glance."
- Business context: explain why a KPI card is valuable (quick visibility, trend awareness, operational monitoring)
- Acceptance criteria (BUSINESS-ONLY, ordered list):
  1. A KPI card for retry success rate is visible on the authorization dashboard
  2. The card displays the metric as a percentage
  3. The card follows the same visual style and layout as existing KPI cards on the dashboard
  4. The data powering the card comes from the authorization metrics
- NO technical hints about React, components, APIs, or file paths
- Priority: Medium, Labels: frontend, dashboard, kpi
- Include a {panel:title=Design Note} stating it should match the existing card pattern

**EPA-backlog-1.wiki** - Backlog: "Add settlement dispute tracking breakdown"
- Realistic payment analytics feature request about breaking down disputes by reason code and outcome
- Brief user story + business context + 3-4 acceptance criteria
- Labels: settlements, reporting

**EPA-backlog-2.wiki** - Backlog: "Implement chargeback alert threshold notifications"
- Realistic feature request for alerting when chargeback rates exceed configurable thresholds
- Brief user story + business context + 3-4 acceptance criteria
- Labels: chargebacks, alerting

**EPA-backlog-3.wiki** - Backlog: "Create funding reconciliation summary report"
- Realistic feature request for a summary report reconciling expected vs actual funding amounts
- Brief user story + business context + 3-4 acceptance criteria
- Labels: funding, reporting, reconciliation

ALL files must be pure Jira wiki markup. No markdown syntax (no ##, no **, no -). Use h2. for headings, # for ordered lists, * for unordered lists, *bold* for bold, {panel} for callouts. No file header comments or metadata lines -- just the wiki content.
  </action>
  <verify>
    <automated>test -d docs/jira && ls docs/jira/*.wiki | wc -l | grep -q "6" && ! grep -l "retry_success_rate\|```\|^##\|^-\ " docs/jira/*.wiki && echo "PASS: 6 wiki files, no markdown syntax, no retry_success_rate" || echo "FAIL"</automated>
  </verify>
  <done>6 Jira .wiki files exist in docs/jira/ with pure wiki markup; TICKET-1 and TICKET-2 stories have business-only acceptance criteria aligned with HANDS_ON_LAB.md tasks; no technical implementation hints in any ticket; 3 backlog items provide board realism</done>
</task>

<task type="auto">
  <name>Task 2: Create Confluence data dictionary .wiki file</name>
  <files>docs/confluence/data-dictionary.wiki</files>
  <action>
Create docs/confluence/ directory and write the data dictionary in Confluence wiki markup:

**data-dictionary.wiki** - "Payment Analytics Data Dictionary"

Structure the page with these sections:

**1. Overview** - Brief description: "This data dictionary documents the metrics, dimensions, and facts available in the Payment Analytics semantic view. Use this reference when querying data through the Cortex Agent or building dashboard components."

**2. Metrics** - Table with columns: ||Metric||Description||Formula||Data Type||Source Domain||
Include ALL 11 existing metrics from the semantic view:
- APPROVAL_RATE: Percentage of authorizations approved | Approved count / Total count * 100 | NUMBER (%) | Authorizations
- TOTAL_AUTHORIZATION_VOLUME: Total authorization amount in USD | SUM(transaction_amount) | NUMBER ($) | Authorizations
- AVERAGE_TRANSACTION_AMOUNT: Average transaction amount in USD | AVG(transaction_amount) | NUMBER ($) | Authorizations
- NET_SETTLEMENT_VOLUME: Total net settlement amount | SUM(net_amount) | NUMBER ($) | Settlements
- TOTAL_DEPOSITS: Total deposit amount | SUM(deposit_amount) | NUMBER ($) | Deposits
- EFFECTIVE_FEE_RATE: Processing fees as percentage of sales | Total fees / Sales amount * 100 | NUMBER (%) | Deposits
- CHARGEBACK_VOLUME: Total chargeback dispute amount | SUM(dispute_amount) | NUMBER ($) | Chargebacks
- CHARGEBACK_WIN_RATE: Percentage of chargebacks won by merchant | Won disputes / Total disputes * 100 | NUMBER (%) | Chargebacks
- CHARGEBACK_RATE: Chargeback count as percentage of total transactions | Chargeback count / Authorization count * 100 | NUMBER (%) | Chargebacks + Authorizations
- NET_ADJUSTMENTS: Net adjustment amount | SUM(adjustment_amount) | NUMBER ($) | Adjustments
- RETRIEVAL_FULFILLMENT_RATE: Percentage of retrieval requests fulfilled | Closed retrievals / Total retrievals * 100 | NUMBER (%) | Retrievals

CRITICAL: Do NOT include retry_success_rate. This omission is deliberate.

**3. Dimensions by Domain** - For each of the 7 MARTS tables, list key dimensions in a table:
||Dimension||Description||Example Values||Domain||
Use the dimension data from RESEARCH.md (transaction_date, merchant_id, card_brand, card_type, entry_mode, etc.)

**4. Fact Columns by Domain** - For each MARTS table, list the numeric fact columns:
||Fact Column||Description||Data Type||Domain||

**5. Notes section** - Use {info} macro: "This dictionary reflects the current semantic view configuration. New metrics should be added to both the semantic view definition and this dictionary."

Read packages/dbt/analyses/payment_analytics_semantic_view.sql to verify exact metric names, descriptions, and formulas. Cross-reference with INFRASTRUCTURE.md for table/dimension accuracy. Use the actual semantic view YAML as the authoritative source -- do not rely solely on the RESEARCH.md summary.

All content must be pure Confluence wiki markup. Use h1., h2. for headings. Use ||Header|| for table headers and |cell| for table cells. Use {info}, {note} macros for callouts. No markdown syntax.
  </action>
  <verify>
    <automated>test -f docs/confluence/data-dictionary.wiki && grep -c "APPROVAL_RATE\|CHARGEBACK_WIN_RATE\|EFFECTIVE_FEE_RATE\|RETRIEVAL_FULFILLMENT_RATE" docs/confluence/data-dictionary.wiki | grep -q "[4-9]" && ! grep -qi "retry_success_rate" docs/confluence/data-dictionary.wiki && ! grep -q "^##\|^-\ \|```" docs/confluence/data-dictionary.wiki && echo "PASS: data dictionary exists, has metrics, no retry, no markdown" || echo "FAIL"</automated>
  </verify>
  <done>Confluence data dictionary .wiki file exists with all 11 existing metrics, key dimensions and facts from all 7 MARTS tables, pure wiki markup, and deliberately omits retry_success_rate</done>
</task>

</tasks>

<verification>
- All 7 .wiki files exist (6 in docs/jira/, 1 in docs/confluence/)
- No file contains markdown syntax (##, **, ```, - lists)
- No Jira ticket contains technical implementation hints (file paths, SQL, component names)
- Data dictionary contains all 11 existing metrics
- Data dictionary does NOT mention retry_success_rate
- TICKET-1 and TICKET-2 acceptance criteria align with HANDS_ON_LAB.md task descriptions
</verification>

<success_criteria>
- 6 Jira .wiki files with pure wiki markup, business-only content
- 1 Confluence .wiki file with complete data dictionary (11 metrics, dimensions, facts)
- retry_success_rate absent from data dictionary
- Content aligned with HANDS_ON_LAB.md task descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/03-generate-reference-content-for-jira-tickets-and-confluence-documentation/03-01-SUMMARY.md`
</output>
