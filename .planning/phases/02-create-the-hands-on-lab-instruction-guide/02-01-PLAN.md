---
phase: 02-create-the-hands-on-lab-instruction-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HANDS_ON_LAB.md
autonomous: true
requirements:
  - HOL-01
  - HOL-02
  - HOL-03

must_haves:
  truths:
    - "Reader understands the medallion data architecture (RAW -> STAGING -> INTERMEDIATE -> MARTS) before starting tasks"
    - "Reader can verify their pre-configured environment is working (Snowflake connection, local app, Cortex Code CLI)"
    - "Reader understands Cortex Code CLI fundamentals (/plan, /new, /model, /mcp) before starting tasks"
    - "Reader knows how to install and configure Jira and Confluence MCP skills"
  artifacts:
    - path: "HANDS_ON_LAB.md"
      provides: "Foundation sections of the HOL guide (header, architecture, setup verification, Cortex Code primer)"
      min_lines: 200
  key_links:
    - from: "HANDS_ON_LAB.md architecture section"
      to: "AGENTS.md data architecture"
      via: "Consistent medallion layer terminology"
      pattern: "RAW.*STAGING.*INTERMEDIATE.*MARTS"
    - from: "HANDS_ON_LAB.md primer section"
      to: "Cortex Code CLI docs"
      via: "Accurate slash commands from 02-RESEARCH.md"
      pattern: "/plan|/new|/mcp|/model"
---

<objective>
Write the foundation sections of HANDS_ON_LAB.md: header with lab overview and time estimates, architecture overview with medallion layer diagram and MARTS table reference, environment setup verification steps, and Cortex Code CLI primer with slash commands and MCP skill installation.

Purpose: Establish participant context and baseline knowledge before the two development tasks. These sections ensure every participant starts with the same understanding of the environment, the data architecture, and the AI coding assistant they will use.

Output: HANDS_ON_LAB.md with Sections 1-3 complete (~25 min of lab time covered).
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-CONTEXT.md
@.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-RESEARCH.md

Key reference files for accurate content:
@AGENTS.md
@packages/dbt/models/marts/payments/authorizations.sql
@packages/dbt/analyses/payment_analytics_semantic_view.sql
@packages/database/utilities/03_create_agent.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write header, architecture overview, and setup verification sections</name>
  <files>HANDS_ON_LAB.md</files>
  <action>
Create HANDS_ON_LAB.md at repo root with the following sections:

**Header block:**
- Lab title: "Hands-On Lab: AI-Assisted SDLC with Cortex Code"
- Total duration estimate (~90 min)
- "What you will build" summary: add a retry success rate metric end-to-end (dbt mart -> semantic view -> Cortex Agent -> frontend KPI card) across two Jira tickets using Cortex Code
- Prerequisites: Snowflake account (pre-configured by instructor), Git, Node.js, WSL (Windows only)
- Note: "The lab environment has been pre-configured using hol_setup.sql. You do not need to run this script."

**Section 1: Architecture Overview (~10 min)**
- Medallion layer ASCII diagram showing RAW -> STAGING (views) -> INTERMEDIATE (dynamic tables) -> MARTS (dynamic tables)
- Table listing all 7 MARTS tables from AGENTS.md: AUTHORIZATIONS, SETTLEMENTS, DEPOSITS, CHARGEBACKS, RETRIEVALS, ADJUSTMENTS, DIM_MERCHANTS
- Brief explanation of the Cortex Agent + Semantic View architecture: semantic view defines queryable metrics over MARTS tables, Cortex Agent uses semantic view for NLQ
- Mention AGENTS.md at repo root is auto-loaded by Cortex Code — no manual config needed
- Diagram showing data flow: dbt models -> Dynamic Tables -> Semantic View -> Cortex Agent -> Frontend
- Keep all information factually accurate to the actual codebase

**Section 2: Environment Setup Verification (~5 min)**
Three verification steps, each with expected output:
1. Confirm Snowflake connection: `snow sql -c ennovate -q "SELECT CURRENT_ROLE(), CURRENT_DATABASE(), CURRENT_SCHEMA();"` — expected: ATTENDEE_ROLE, COCO_SDLC_HOL, MARTS
2. Confirm local app runs: `cd apps/frontend && npm install && npm run dev` — expected: Next.js dev server at http://localhost:3000 showing dashboard with real data
3. Confirm Cortex Code CLI installed: `cortex --version` — expected: version output. If not installed, provide install command: `curl -LsS https://ai.snowflake.com/static/cc-scripts/install.sh | sh`
- Windows note: "If you are on Windows, you must use WSL. Open a WSL terminal before running commands."

Voice: Third person instructional ("In this section, you will..."). Every command copy-paste ready. Include context explaining why each step matters.
  </action>
  <verify>
    <automated>test -f HANDS_ON_LAB.md && grep -q "Architecture Overview" HANDS_ON_LAB.md && grep -q "Setup Verification" HANDS_ON_LAB.md && grep -q "AUTHORIZATIONS" HANDS_ON_LAB.md && grep -q "clnt_id" HANDS_ON_LAB.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>HANDS_ON_LAB.md exists with header, architecture overview (medallion diagram, 7 MARTS tables, Cortex Agent overview), and setup verification (3 steps with expected outputs). All commands are copy-paste ready. Windows WSL note included.</done>
</task>

<task type="auto">
  <name>Task 2: Write Cortex Code primer section with MCP skill setup</name>
  <files>HANDS_ON_LAB.md</files>
  <action>
Append Section 3 to HANDS_ON_LAB.md:

**Section 3: Cortex Code Primer (~10 min)**

3.1 What is Cortex Code:
- Snowflake's AI coding assistant (GA February 2, 2026), built on Claude Code foundation
- Runs in terminal, reads repo context automatically via AGENTS.md
- Supports Snowflake-native features: SQL execution, dbt skills, RBAC awareness

3.2 Key slash commands reference table:
| Command | Purpose |
|---------|---------|
| `/plan` | Enable plan mode — Cortex Code shows action plan before executing |
| `/plan-off` | Disable plan mode — return to direct execution |
| `/new` | Start fresh conversation (clears context) |
| `/model` | Select AI model (auto, Opus 4.6, Sonnet 4.6, etc.) |
| `/mcp` | Manage MCP integrations |
| `/status` | Show current session status |

Note: "Plan mode is session-scoped. After `/new`, you must re-enable `/plan` if you want plan mode in the new session."

3.3 Install Jira MCP skill:
```
/mcp
```
Then follow prompts to add Jira server, OR use CLI:
```bash
cortex mcp add jira --url https://your-org.atlassian.net --auth-token <token>
```
Note: "Your instructor will provide the Jira instance URL and API token."

3.4 Install Confluence MCP skill:
Same pattern as Jira:
```bash
cortex mcp add confluence --url https://your-org.atlassian.net --auth-token <token>
```

3.5 Quick test — verify Cortex Code reads repo context:
Launch `cortex` from repo root, then ask: "What database and schema does this project use?"
Expected: Cortex Code references COCO_SDLC_HOL and the medallion architecture from AGENTS.md.

Include note: "Suggested prompts throughout this lab are starting points — feel free to rephrase in your own words. Cortex Code understands natural language variations."

All commands from 02-RESEARCH.md official sources. Voice: third person instructional. Time estimate shown in section header.
  </action>
  <verify>
    <automated>grep -q "Cortex Code Primer" HANDS_ON_LAB.md && grep -q "/plan" HANDS_ON_LAB.md && grep -q "/new" HANDS_ON_LAB.md && grep -q "mcp add jira" HANDS_ON_LAB.md && grep -q "mcp add confluence" HANDS_ON_LAB.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Section 3 appended with Cortex Code overview, slash command reference table, Jira and Confluence MCP setup instructions, and quick verification test. All commands sourced from official docs (02-RESEARCH.md). Suggested prompts framed as starting points per locked decision.</done>
</task>

</tasks>

<verification>
- HANDS_ON_LAB.md exists at repo root
- Contains header with lab title, duration, and prerequisites
- Section 1 has medallion architecture diagram and all 7 MARTS tables
- Section 2 has 3 verification steps with expected outputs and Windows WSL note
- Section 3 has slash command table, MCP setup for Jira + Confluence, and verification test
- Voice is consistently third person instructional
- All commands are copy-paste ready
- Time estimates shown per section
</verification>

<success_criteria>
HANDS_ON_LAB.md foundation sections (1-3) are complete and self-contained. A participant reading only these sections would understand the architecture, verify their environment, and know how to use Cortex Code CLI before starting development tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-01-SUMMARY.md`
</output>
