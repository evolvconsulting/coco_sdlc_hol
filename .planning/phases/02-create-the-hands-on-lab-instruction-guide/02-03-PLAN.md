---
phase: 02-create-the-hands-on-lab-instruction-guide
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - HANDS_ON_LAB.md
autonomous: false
requirements:
  - HOL-07
  - HOL-08
  - HOL-09

must_haves:
  truths:
    - "Reader can follow step-by-step instructions to add a KPI card to the frontend using the existing KPICard component pattern"
    - "Reader can verify the new KPI card appears in the local app at http://localhost:3000"
    - "Reader can create a pull request using git and gh CLI"
    - "Reader has a troubleshooting reference for common issues"
    - "Guide is complete end-to-end: a participant can work through the entire document without gaps"
  artifacts:
    - path: "HANDS_ON_LAB.md"
      provides: "Complete HOL guide with all 7 sections"
      min_lines: 600
  key_links:
    - from: "HANDS_ON_LAB.md Task 2 steps"
      to: "apps/frontend/src/app/analytics/authorization/page.tsx"
      via: "Guide describes adding KPICard to this file"
      pattern: "KPICard|retrySuccessRate"
    - from: "HANDS_ON_LAB.md Task 2 type update"
      to: "apps/frontend/src/types/domain.ts"
      via: "Guide describes adding retrySuccessRate to AuthorizationKPIs"
      pattern: "AuthorizationKPIs|retrySuccessRate"
    - from: "HANDS_ON_LAB.md appendix"
      to: "Troubleshooting solutions"
      via: "Common issues and fixes"
      pattern: "Troubleshooting|WSL|dynamic table|semantic view"
---

<objective>
Write the Task 2 walkthrough (Section 6: KPI Card), wrap-up (Section 7), and appendix with troubleshooting. Then perform a human verification checkpoint to confirm the complete guide reads well and is accurate.

Purpose: Complete the HOL guide with the second development task (frontend changes), a summary wrap-up, and reference material for participants who hit issues.

Output: Complete HANDS_ON_LAB.md with all 7 sections + appendix. Human-verified.
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-CONTEXT.md
@.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-RESEARCH.md
@.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-01-SUMMARY.md
@.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-02-SUMMARY.md

Key reference files for Task 2 content:
@apps/frontend/src/app/analytics/authorization/page.tsx
@apps/frontend/src/components/ui/KPICard.tsx
@apps/frontend/src/types/domain.ts
@apps/frontend/src/app/api/analytics/authorization/kpis/route.ts

<interfaces>
<!-- Key patterns the executor needs for accurate Task 2 guide content -->

From apps/frontend/src/components/ui/KPICard.tsx — KPICard component props:
- title: string
- value: number
- format: "number" | "currency" | "percent"
- description: string
- loading: boolean
- color: string (hex)

From apps/frontend/src/types/domain.ts — AuthorizationKPIs interface:
- Existing fields: totalTransactions, approvedCount, declinedCount, approvalRate, totalAmount, approvedAmount, avgTicketSize, trends
- New field to add: retrySuccessRate: number

From 02-RESEARCH.md — KPI card JSX pattern:
```tsx
<Col xs={24} sm={12} lg={6}>
  <KPICard
    title="Retry Success Rate"
    value={kpiData?.retrySuccessRate ?? 0}
    format="percent"
    description="Percentage of declined transactions that succeeded on retry"
    loading={kpis.isLoading}
    color="#52c41a"
  />
</Col>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Task 2 walkthrough, wrap-up, and appendix</name>
  <files>HANDS_ON_LAB.md</files>
  <action>
Append Sections 6, 7, and Appendix to HANDS_ON_LAB.md:

**Section 6: Task 2 — Add KPI Card to Dashboard (~20 min)**

Brief intro: "In this task, you will add a KPI card to the authorization dashboard that displays the retry success rate metric you created in Task 1. This demonstrates a frontend change driven by the backend metric you just built."

**Step 6.1: Read the Jira ticket**
Suggested prompt:
> Show me Jira ticket [TICKET-2]. What does it ask me to implement?
"[TICKET-2] is a placeholder — your instructor will provide the actual ticket ID."

**Step 6.2: Create a new branch**
```bash
git checkout -b feature/retry-success-kpi-card
```

**Step 6.3: Enable plan mode and describe the task**
```
/plan
```
Suggested prompt (from 02-RESEARCH.md):
> Read Jira ticket [TICKET-2]. Look at apps/frontend/src/app/analytics/authorization/page.tsx, apps/frontend/src/components/ui/KPICard.tsx, and apps/frontend/src/types/domain.ts. Add a KPI card that shows the retry_success_rate from the authorization KPIs API. Follow the exact same pattern as the existing KPI cards.

"Review the plan. Confirm it includes changes to:"
- `apps/frontend/src/types/domain.ts` — add `retrySuccessRate` to `AuthorizationKPIs` interface
- `apps/frontend/src/app/api/analytics/authorization/kpis/route.ts` — add SQL column + return field
- `apps/frontend/src/app/analytics/authorization/page.tsx` — add new `<Col>` + `<KPICard>`

"IMPORTANT: The TypeScript interface must be updated BEFORE the API route and page component. If Cortex Code's plan shows the page change first, ask it to reorder."

**Step 6.4: Execute the plan**
> The plan looks good. Execute it.

Show expected changes per file:

a) `domain.ts` — add `retrySuccessRate: number` to AuthorizationKPIs interface

b) `kpis/route.ts` — add to SQL SELECT:
```sql
ROUND(
  SUM(CASE WHEN retry_success_flag = 1 THEN 1.0 ELSE 0 END) * 100.0 /
  NULLIF(SUM(CASE WHEN retry_attempt_flag = 1 THEN 1 ELSE 0 END), 0),
2) as retry_success_rate
```
And add to return object:
```typescript
retrySuccessRate: Number(row.RETRY_SUCCESS_RATE) || 0,
```

c) `page.tsx` — add after existing KPI cards in the `<Row gutter={[16, 16]}>` block:
```tsx
<Col xs={24} sm={12} lg={6}>
  <KPICard
    title="Retry Success Rate"
    value={kpiData?.retrySuccessRate ?? 0}
    format="percent"
    description="Percentage of declined transactions that succeeded on retry"
    loading={kpis.isLoading}
    color="#52c41a"
  />
</Col>
```

**Step 6.5: Verify locally**
"Start the dev server if not already running:"
```bash
cd apps/frontend && npm run dev
```
"Open http://localhost:3000/analytics/authorization"
"You should see a new 'Retry Success Rate' KPI card alongside the existing authorization KPIs."

**Step 6.6: Commit and push**
```bash
git add apps/frontend/
git commit -m "feat(frontend): add retry success rate KPI card to authorization dashboard"
git push -u origin feature/retry-success-kpi-card
```

**Step 6.7: Create a pull request**
```bash
gh pr create --title "Add retry success rate KPI card" \
  --body "Adds Retry Success Rate metric to the authorization dashboard as a KPI card. Reads from the retry_success_rate field added in the previous ticket."
```
"Or use Cortex Code:"
> Create a GitHub pull request for this branch. Title: "Add retry success rate KPI card". Describe what was changed and why.

---

**Section 7: Wrap-up (~5 min)**

"Congratulations! In this lab, you completed two full development cycles using Cortex Code:"

Summary of what was accomplished:
- Task 1: Added a new business metric (retry success rate) end-to-end through the data stack — dbt intermediate model, marts table, semantic view, Cortex Agent instructions
- Task 2: Added a frontend KPI card to visualize the new metric, following existing component patterns
- Along the way: Read Jira tickets, planned work with AI, executed changes, verified results, committed code, updated documentation, and created a pull request

"Key takeaways:"
1. Cortex Code reads your repo context (AGENTS.md) automatically — no manual setup
2. Plan mode (/plan) lets you review changes before they happen
3. MCP skills connect Cortex Code to Jira and Confluence for full SDLC coverage
4. AI-assisted development works best when you extend existing patterns, not start from scratch
5. Context hygiene matters — use /new between tasks to keep context focused

---

**Appendix**

**A. Troubleshooting**
Table of common issues and solutions:

| Issue | Cause | Solution |
|-------|-------|----------|
| `cortex: command not found` | CLI not installed or not in PATH | Run install script: `curl -LsS https://ai.snowflake.com/static/cc-scripts/install.sh \| sh` then restart terminal |
| Windows install fails | Cortex Code requires WSL on Windows | Open WSL terminal, then run install |
| Dynamic table shows old data | Dynamic tables refresh on schedule, not on DDL change | Run `ALTER DYNAMIC TABLE ... REFRESH;` manually |
| Semantic view metric not found | YAML updated but view not rebuilt | Run `CALL SYSTEM$CREATE_SEMANTIC_VIEW_FROM_YAML(...)` |
| KPI card shows 0 or undefined | TypeScript interface not updated | Add `retrySuccessRate: number` to `AuthorizationKPIs` in `domain.ts` |
| Cortex Agent gives generic answer | Agent instructions not updated | Recreate agent with updated `03_create_agent.sql` |
| /plan mode off after /new | Plan mode is session-scoped | Re-enable with `/plan` in each new session |
| Verification query returns empty | Missing clnt_id filter | Add `WHERE clnt_id = 'dmcl'` to all queries |

**B. Resources**
- Cortex Code CLI docs: `docs.snowflake.com/en/user-guide/cortex-code/cortex-code-cli`
- Cortex Code CLI reference: `docs.snowflake.com/en/user-guide/cortex-code/cli-reference`
- Cortex Code extensibility (MCP): `docs.snowflake.com/en/user-guide/cortex-code/extensibility`
- dbt documentation: `docs.getdbt.com`
- Snowflake Dynamic Tables: `docs.snowflake.com/en/user-guide/dynamic-tables`

**C. Glossary**
- **Medallion architecture**: Data pipeline pattern with RAW (bronze), STAGING (silver), INTERMEDIATE/MARTS (gold) layers
- **Dynamic table**: Snowflake table that auto-refreshes based on upstream changes (configured target lag)
- **Semantic view**: YAML-defined metadata layer over tables that enables natural language queries
- **Cortex Agent**: Snowflake AI agent that answers questions using semantic view metadata
- **MCP (Model Context Protocol)**: Standard for connecting AI tools to external services (Jira, Confluence, etc.)
- **Plan mode**: Cortex Code mode where changes are proposed and reviewed before execution

Voice: third person instructional throughout. All commands copy-paste ready. Time estimates in section headers.
  </action>
  <verify>
    <automated>grep -q "Task 2" HANDS_ON_LAB.md && grep -q "KPICard" HANDS_ON_LAB.md && grep -q "retrySuccessRate" HANDS_ON_LAB.md && grep -q "Wrap-up" HANDS_ON_LAB.md && grep -q "Troubleshooting" HANDS_ON_LAB.md && grep -q "Glossary" HANDS_ON_LAB.md && grep -q "gh pr create" HANDS_ON_LAB.md && wc -l < HANDS_ON_LAB.md | awk '{if ($1 >= 500) print "PASS"; else print "FAIL — only " $1 " lines"}'</automated>
  </verify>
  <done>Sections 6-7 and Appendix complete. Task 2 covers all 7 steps from CONTEXT.md (ticket -> branch -> plan -> execute -> verify -> commit -> PR). TypeScript-first ordering noted. Wrap-up summarizes achievements and takeaways. Appendix has troubleshooting table (8 issues), resources, and glossary. Guide is 500+ lines total.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete HANDS_ON_LAB.md guide</name>
  <files>HANDS_ON_LAB.md</files>
  <action>Present the complete HANDS_ON_LAB.md to the user for review. The guide should have all 7 sections plus appendix. No code changes needed — this is a review checkpoint.</action>
  <verify>
    <automated>test -f HANDS_ON_LAB.md && wc -l < HANDS_ON_LAB.md | awk '{if ($1 >= 500) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>User has reviewed and approved the complete HANDS_ON_LAB.md guide.</done>
  <what-built>Complete HANDS_ON_LAB.md guide with all 7 sections plus appendix, covering two full SDLC cycles using Cortex Code CLI against the payment analytics codebase.</what-built>
  <how-to-verify>
    1. Open HANDS_ON_LAB.md in a markdown preview (VS Code or GitHub)
    2. Verify the document structure follows the locked decision order:
       - Header with duration and prerequisites
       - Section 1: Architecture Overview (~10 min)
       - Section 2: Environment Setup Verification (~5 min)
       - Section 3: Cortex Code Primer (~10 min)
       - Section 4: Task 1 — Retry Success Rate (~30 min)
       - Section 5: Context Switch (~2 min)
       - Section 6: Task 2 — KPI Card (~20 min)
       - Section 7: Wrap-up (~5 min)
       - Appendix (Troubleshooting, Resources, Glossary)
    3. Spot-check: Are all commands copy-paste ready?
    4. Spot-check: Are Jira/Confluence URLs parameterized as placeholders?
    5. Spot-check: Does the verification SQL include `WHERE clnt_id = 'dmcl'`?
    6. Spot-check: Is the voice consistently third person instructional?
    7. Confirm no `dbt build` instructions (anti-pattern)
    8. Read the troubleshooting table — are the 8 issues realistic?
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- HANDS_ON_LAB.md exists at repo root with all 7 sections + appendix
- Section 6 follows exact 7-step flow from CONTEXT.md for Ticket 2
- TypeScript interface update ordered before API route and page component
- KPICard usage matches established codebase pattern (Col grid, props)
- PR creation shown via both gh CLI and Cortex Code
- Section 7 summarizes both tasks with 5 key takeaways
- Appendix covers 8 troubleshooting issues from RESEARCH.md pitfalls
- Total guide is 500+ lines
- Human has verified the complete guide
</verification>

<success_criteria>
HANDS_ON_LAB.md is complete, human-verified, and ready for use. A participant can work through the entire document from start to finish, completing two full SDLC cycles using Cortex Code, without encountering gaps or ambiguities.
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-the-hands-on-lab-instruction-guide/02-03-SUMMARY.md`
</output>
