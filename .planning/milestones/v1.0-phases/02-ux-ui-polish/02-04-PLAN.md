---
phase: 02-ux-ui-polish
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06

must_haves:
  truths:
    - "Navigation sidebar submenu stays expanded when navigating between any two /analytics/* pages"
    - "'Saved Reports' is absent from the sidebar nav"
    - "All 6 domain pages show KPICard for KPI metrics (consistent card style — no raw Statistic components visible)"
    - "Changing the date range on any domain page updates the displayed data without a full page reload"
    - "No blank loading spinners appear over chart areas — pulsing Skeleton placeholders appear instead"
    - "Charts on all 6 domain pages display readable axis labels (compact currency or count format)"
    - "All 6 domain pages render without horizontal scroll at 1440px desktop width"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of all Phase 2 polish changes across the running portal.

Purpose: Code changes across plans 02-01, 02-02, and 02-03 produce correct automated output, but visual consistency — navigation behavior, KPI card uniformity, skeleton animation quality, chart readability, date filter responsiveness, and desktop layout — requires human eyes. This plan captures the final sign-off.

Output: Human confirmation that all 6 UX requirements are satisfied. Phase 2 is complete on "approved" signal.
</objective>

<execution_context>
@C:/Users/TrentFoley/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/TrentFoley/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ux-ui-polish/02-01-SUMMARY.md
@.planning/phases/02-ux-ui-polish/02-02-SUMMARY.md
@.planning/phases/02-ux-ui-polish/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run automated pre-checks</name>
  <files></files>
  <action>
    Start the Next.js dev server if not already running:
    ```bash
    cd /c/Users/TrentFoley/Source/coco_sdlc_hol/apps/frontend && npm run dev
    ```
    Wait up to 30 seconds for "Ready" output.

    Then run the following automated checks and print all results:

    1. Verify sidebar fix is in place:
    ```bash
    grep -c "openKeys={openKeys}" apps/frontend/src/components/layout/DashboardLayout.tsx
    ```
    Expected: 1

    2. Verify Saved Reports is gone:
    ```bash
    grep -c "Saved Reports" apps/frontend/src/components/layout/DashboardLayout.tsx
    ```
    Expected: 0

    3. Verify formatters module is present:
    ```bash
    grep -c "export const format" apps/frontend/src/lib/formatters.ts
    ```
    Expected: 5

    4. Verify no Statistic components remain on any domain page:
    ```bash
    grep -rn "Statistic" apps/frontend/src/app/analytics/
    ```
    Expected: no matches (or matches only in import statements that are unused — flag these)

    5. Verify no Spin overlays remain on domain page overview sections:
    ```bash
    grep -rn "<Spin" apps/frontend/src/app/analytics/
    ```
    Expected: at most one match (authorization details tab — acceptable). Zero is also fine.

    6. Verify all 6 domain pages import KPICard:
    ```bash
    grep -rn "KPICard" apps/frontend/src/app/analytics/
    ```
    Expected: matches in all 6 page files

    7. Check dev server is responding:
    ```bash
    curl -s http://localhost:3000 | head -c 200
    ```
    Expected: HTML response (not an error)

    Print a clear PASS/FAIL summary for each check before handing off to human verification.
    If any check FAILS, note it clearly — the human tester should look specifically at the failing area.
  </action>
  <verify>grep -c "openKeys={openKeys}" apps/frontend/src/components/layout/DashboardLayout.tsx</verify>
  <done>All 7 automated checks run and results printed. Dev server is responding at http://localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 UX/UI polish across all 6 analytics domain pages:
    - Sidebar fix: Analytics submenu now uses controlled openKeys (stays expanded on direct navigation)
    - "Saved Reports" stub removed from sidebar
    - Canonical formatter library created (formatCompactCurrency, formatPercent, etc.)
    - Chargeback, retrieval, adjustment pages: converted Statistic → KPICard, Spin → Skeleton
    - Authorization, settlement, funding pages: Spin → Skeleton, ad-hoc formatters → canonical imports; settlement + funding also converted to KPICard
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in your browser at 1440px wide (or maximize a desktop window).

    **Step 1 — Navigation (UX-01)**
    1. Click "Dashboard" in the sidebar
    2. Click "Authorization" in the Analytics submenu — does the submenu STAY expanded? (not collapse)
    3. Directly navigate in the browser address bar to http://localhost:3000/analytics/chargeback — does the Analytics submenu auto-expand?
    4. Confirm "Saved Reports" does NOT appear anywhere in the sidebar

    PASS if: submenu stays expanded in all navigation scenarios; Saved Reports is absent.

    **Step 2 — Chart formatting (UX-02)**
    1. Go to http://localhost:3000/analytics/authorization
    2. Look at the y-axis labels on any timeseries or bar chart — are they compact with currency symbol? (e.g., "$1.2M", "$450K")
    3. Go to http://localhost:3000/analytics/chargeback
    4. Check chart y-axis labels for compact format

    PASS if: y-axis labels show compact values (not raw large numbers like "1234567").

    **Step 3 — Loading states (UX-03)**
    1. On any domain page, trigger a reload or change the date range
    2. Do you see a pulsing skeleton placeholder in place of charts during loading?
    3. Does the page layout remain stable (no jumping) when the skeleton transitions to real content?
    4. Do KPI cards show a skeleton/loading animation while data is fetching?

    PASS if: Skeleton placeholders visible during load; no layout shift; no spinner overlays on chart areas.

    **Step 4 — Date pickers (UX-04)**
    1. Go to http://localhost:3000/analytics/settlement
    2. Change the end date in the date range picker to 2 months earlier
    3. Do the KPI values and charts update immediately (without pressing Refresh)?
    4. Check http://localhost:3000/analytics/funding — same test

    PASS if: Data updates automatically when date range changes (React Query refetch triggered by param change).

    **Step 5 — Domain filters (UX-05)**
    1. On the Authorization page, if a brand/status filter dropdown is present, change the selected value
    2. Verify the data updates (or is clearly filtered)
    3. Check the Chargeback page for reason code filter behavior
    4. Check the Retrieval page — if a filter is present, test it; if it was removed, confirm it's gone

    PASS if: Filters that exist update the displayed data; no orphaned filters (UI elements that change nothing).

    **Step 6 — Desktop layout (UX-06)**
    1. At 1440px wide, check each of the 6 domain pages:
       - http://localhost:3000/analytics/authorization
       - http://localhost:3000/analytics/settlement
       - http://localhost:3000/analytics/funding
       - http://localhost:3000/analytics/chargeback
       - http://localhost:3000/analytics/retrieval
       - http://localhost:3000/analytics/adjustment
    2. Do KPI cards fit in a consistent row without wrapping unexpectedly?
    3. Is there any horizontal scroll bar on any page?
    4. Does any content appear cut off or overflow its container?

    PASS if: All 6 pages render cleanly at desktop width; no overflow; KPI card rows look consistent.

    **Step 7 — Overall KPI card visual consistency (UX-01)**
    Compare KPI cards side-by-side across any two domain pages (e.g., authorization vs chargeback):
    - Do they have the same card style, padding, and font sizes?
    - Is the loading animation consistent?

    PASS if: KPI cards look visually identical in structure across all pages.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 steps PASS.
    Or type "issues: [describe each issue]" and Claude will triage and fix.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 is complete when:
1. All 7 automated pre-checks in Task 1 PASS
2. Human tester types "approved" after verifying all 6 UX verification steps
3. No outstanding issues from human review
</verification>

<success_criteria>
- Navigation: Analytics submenu stays expanded regardless of how user arrives at domain pages
- Charts: Y-axis shows compact formatted values on all 6 domain pages
- Loading: Skeleton placeholders visible during data fetch; no Spin overlays on chart areas
- Filters: Date pickers and domain filters trigger data refresh without manual Refresh button
- Layout: All 6 pages render at 1440px width without overflow or scroll
- Consistency: KPI cards look structurally identical across all domain pages
- All 6 requirements (UX-01 through UX-06) satisfied and signed off by human tester
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-ui-polish/02-04-SUMMARY.md`

Include in the summary:
- Results of all 7 automated pre-checks (PASS/FAIL)
- Human tester's verification signal and any issues they flagged
- Any follow-up fixes made in response to human review
- Confirmation that all 6 UX requirements are now COMPLETE
- Overall Phase 2 status: COMPLETE or issues remaining

Also update .planning/REQUIREMENTS.md to mark UX-01 through UX-06 as complete.
Also update .planning/STATE.md to reflect Phase 2 complete and ready for Phase 3.
Also update .planning/ROADMAP.md Phase 2 status row to Complete with today's date.
</output>
