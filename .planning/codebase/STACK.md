# Technology Stack

**Analysis Date:** 2026-02-28

## Languages

**Primary:**
- TypeScript 5.x - Frontend application, API routes, configuration
- SQL (Snowflake dialect) - Data transformation via dbt and API queries
- YAML - dbt configuration and Snowflake profiles

**Secondary:**
- JavaScript - Build configuration (ESM modules)
- HTML/CSS - Generated by React components and Tailwind

## Runtime

**Environment:**
- Node.js (version unspecified, 18.x or 20.x inferred from Next.js 16 compatibility)
- Next.js 16.1.6 (React Server Components, API Routes, SSR)

**Package Manager:**
- npm (package-lock.json format)
- Lockfile: `package-lock.json` present in root and `apps/frontend/`

## Frameworks

**Core:**
- Next.js 16.1.6 - Full-stack web framework with API routes, server components, and middleware
- React 19.2.3 - UI component library
- React DOM 19.2.3 - DOM rendering

**UI Components & Styling:**
- Ant Design (antd) 6.3.0 - Enterprise component library
- Ant Design Icons 6.1.0 - Icon set
- Tailwind CSS 4 - Utility-first CSS framework
- TailwindCSS PostCSS plugin 4.x - PostCSS integration for Tailwind

**Visualization:**
- ECharts 6.0.0 - Data visualization library (charts, graphs)
- echarts-for-react 3.0.6 - React bindings for ECharts
- Vega 6.2.0 - Grammar of graphics library
- Vega-Lite 6.4.2 - High-level grammar of graphics
- react-vega 8.0.0 - React integration for Vega

**Data & State Management:**
- TanStack React Query 5.90.21 - Server state management, caching, and data fetching
- ioredis 5.9.3 - Redis client (available in dependencies, usage pattern unclear)

**Utilities:**
- dayjs 1.11.19 - Date manipulation library (lightweight alternative to moment.js)
- react-markdown 10.1.0 - Render Markdown as React components
- remark-gfm 4.0.1 - GitHub Flavored Markdown support plugin
- uuid 13.0.0 - Unique ID generation
- xlsx 0.18.5 - Excel file parsing and generation
- pptxgenjs 4.0.1 - PowerPoint presentation generation (root package)

**Database & Data:**
- snowflake-sdk 2.3.4 - Snowflake Node.js connector for executing queries

## Development Tools

**Linting & Code Quality:**
- ESLint 9.x - Static code analysis
- eslint-config-next 16.1.6 - Next.js ESLint configuration and rules

**Type Checking:**
- TypeScript 5.x - Static type checking with strict mode enabled

**Build & Development:**
- PostCSS 4.x - CSS transformation framework (via Tailwind)

**Testing & Browser Automation:**
- Puppeteer 24.37.5 - Headless browser automation (screenshot/PDF generation capability)

**Ant Design Registry:**
- @ant-design/nextjs-registry 1.3.0 - Server-side rendering support for Ant Design with Next.js

## Key Dependencies

**Critical:**
- snowflake-sdk 2.3.4 - Direct database connectivity; all analytics APIs depend on this for executing Snowflake queries
- next 16.1.6 - Core framework; responsible for routing, API endpoints, SSR, and server-side features
- @tanstack/react-query 5.90.21 - Essential for client-side data fetching, caching, and synchronization across pages

**Data Transformation:**
- dbt-labs/dbt_utils 1.x-2.x (managed in `packages/dbt/packages.yml`) - Common macros and utilities for dbt models

**Visualization & Rendering:**
- antd 6.3.0 - Components for dashboard UI, tables, buttons, and form controls
- echarts 6.0.0 - Visualization engine for analytics charts and KPI displays
- react-markdown 10.1.0 - Render documentation and formatted text

## Configuration

**Environment:**
- `.env.local` file (not committed) - Contains Snowflake credentials and configuration
- See `.env.example` at `apps/frontend/.env.example` for required variables
- Support for both key-pair and password authentication to Snowflake

**TypeScript:**
- `apps/frontend/tsconfig.json` - Strict mode, target ES2017, module resolution bundler
- Path aliases configured: `@/*` maps to `./src/*`
- JSX preset: react-jsx (automatic runtime)

**Build Configuration:**
- `apps/frontend/next.config.ts` - Minimal Next.js configuration
- `apps/frontend/postcss.config.mjs` - PostCSS setup with Tailwind CSS plugin
- `apps/frontend/eslint.config.mjs` - ESLint with Next.js core-web-vitals and TypeScript support

**dbt Configuration:**
- `packages/dbt/dbt_project.yml` - dbt project metadata, model materialization strategy (views, dynamic tables)
- `packages/dbt/packages.yml` - dbt package dependencies
- `packages/dbt/profiles.yml.example` - Snowflake connection profile template (expected at `~/.dbt/profiles.yml`)

## Platform Requirements

**Development:**
- Node.js 18+ (based on dependencies and Next.js 16 compatibility)
- npm 8+ (uses package-lock.json v3)
- Snowflake account with COCO_SDLC_HOL database
- Read access to MARTS, STAGING, INTERMEDIATE schemas

**Production:**
- Deployment target: SPCS
- Snowflake account connectivity required (JWT or password authentication)
- Environment variables for Snowflake credentials (SNOWFLAKE_ACCOUNT, SNOWFLAKE_USER, SNOWFLAKE_WAREHOUSE, SNOWFLAKE_DATABASE, SNOWFLAKE_SCHEMA, and one of: SNOWFLAKE_PRIVATE_KEY, SNOWFLAKE_PRIVATE_KEY_PATH, or SNOWFLAKE_PASSWORD)

**Database:**
- Snowflake (production data warehouse)
- Database: COCO_SDLC_HOL
- Schemas: MARTS (queries), STAGING, INTERMEDIATE (dbt models)
- Warehouse: COMPUTE_WH

---

*Stack analysis: 2026-02-28*
